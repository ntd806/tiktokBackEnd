export const DESTINATION = {
    BANNER: './public/image/banner',
    IMAGE: './public/image',
}

export const IMAGE_PATH_SERVER = {
    IMAGE: `${process.env.URL_DOMAIN_SERVER}/image`,
    BANNER: `${process.env.URL_DOMAIN_SERVER}/image/banner`,
}